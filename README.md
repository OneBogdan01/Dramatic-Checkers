# Hammered game template

A starting point for a game built on the [Hammered engine](https://github.com/OneBogdan01/hammered).


## Web build 

```bash
emcmake cmake -B build-web -D CMAKE_BUILD_TYPE=Debug # or whatever type
cmake --build build-web      # produces index.html using the engine's shell.html
```
